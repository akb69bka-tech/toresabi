# 株式自動売買 自走エンジン 運用ガイド

ブラウザ版（`株式自動売買システム.html`）で検証した戦略を、そのまま毎日自動で動かすための仕組みです。
**ブラウザ版と自走エンジンの売買判定は完全に同一**であることをテストで保証しています（同じデータを与えると取引履歴・資産曲線・コストが一致します）。

---

## 1. できること・できないこと

| できること | できないこと |
|---|---|
| 毎日夕方に株価を取得し、全監視銘柄をスクリーニング | 株価の予測。「学習」は戦略パラメータの再検証であり、値動きを当てる機能ではありません |
| ブラウザ版と同一ロジックで売買を判定し、翌朝の注文を確定 | 利益の保証。擬似データでは上昇相場で買い持ちに負けることが分かっています |
| 週1回、ウォークフォワード検証つきで設定を再学習 | 単元未満株での実発注（kabuステーションAPIが未対応） |
| kabuステーションAPI（auカブコム証券）への発注 | 他社（SBI・楽天など）への直接発注。API が公開されていません |
| 多重の安全装置と緊急停止 | 端末の電源が切れている間の運用（PC の常時起動が必要） |

### 重要な制約

- **kabuステーションAPI は Windows 専用**で、kabuステーションを起動した PC 上で動かす必要があります。
- **単元未満株は API 経由で売買できません。** 実発注では `risk.unit: 100` にする必要があり、50万円だと株価1,000円未満の銘柄に限られます（スクリーナーが自動で絞ります）。
- 株価データの無料ソース（Stooq）は終値確定後の夕方に更新されます。本システムは終値で判定して翌朝の寄付で発注する設計なので、これで足ります。
- このエンジンの証券会社接続部分は、**模擬サーバに対するテストのみ**で、実口座では未検証です。必ず検証環境（ポート 18081）と `live-dryrun` で確認してから使ってください。

---

## 2. セットアップ

```bash
# Python 3.11 以上
pip install -r requirements.txt
python run.py init        # config.yaml と universe.csv（主要銘柄のサンプル）を作る
python run.py demo        # 過去データで運用を再生して成績を見る
```

`universe.csv` は監視する銘柄の一覧です（`code,name` の CSV）。JPX の「東証上場銘柄一覧」を変換すれば全上場銘柄を監視できます。銘柄数が多いほど株価取得に時間がかかります（Stooq は1銘柄あたり約0.6秒、4,000銘柄で約40分）。

---

## 3. 段階的に進める（この順番を守ってください）

```
demo  →  paper  →  live-dryrun  →  live
```

### 段階1: demo（過去の再生）

```bash
python run.py demo --days 250
python run.py backtest --days 500
python run.py learn
```

まず「買い持ちとの差」を見てください。買い持ちに負けている設定で自走させても意味がありません。

### 段階2: paper（実データ・仮想約定）

`config.yaml` の `mode: paper` のまま、毎日動かします。

```bash
python run.py loop          # 常駐して 18:00 に判定、翌 08:30 に(paper では)記録
python run.py dashboard     # 別の窓で http://127.0.0.1:8765/ を開く
```

または Windows のタスクスケジューラ／cron で `python run.py cycle` を毎日 18:00 に実行します。
**最低 20 営業日**はこの段階で動かし、ダッシュボードの資産推移とログに不審な動きがないか確認してください。この実績日数がないと `live` は起動を拒否します。

### 段階3: live-dryrun（証券会社に接続するが発注しない）

kabuステーションを起動し、API 設定で API パスワードを発行します。`config.yaml`:

```yaml
mode: live-dryrun
risk:
  unit: 100            # 必須。単元未満株は API で扱えません
broker:
  type: kabu
  kabu:
    base_url: http://localhost:18081/kabusapi   # まず検証環境
    api_password: "発行したAPIパスワード"
    order_password: "取引パスワード"
```

```bash
python run.py cycle      # 判定
python run.py orders     # → ログに [試運転] 発注しない ... と出れば正常
```

接続・認証・注文内容の組み立てまでを実際に行い、送信だけしません。数日続けて、ログの注文内容が妥当かを目で確認してください。

### 段階4: live（実発注）

以下の**すべて**を満たさないと発注しません。

1. `mode: live`
2. `live.enabled: true`
3. `live.confirm_phrase` に `私は自己責任で実発注を許可します` と正確に記入
4. ペーパー運用の実績が `live.min_paper_days`（既定 20 日）以上
5. `risk.unit` が 100 以上
6. 緊急停止ファイル `STOP` が存在しない
7. 発注時刻が `guard.send_window`（既定 08:00〜08:55）の範囲内
8. 1注文の金額が `guard.max_order_value_yen` 以下、1日の発注回数が `guard.max_daily_orders` 以下

最初は `guard.max_order_value_yen` を小さく（例: 5万円）して、**少額で数週間**動かしてください。

---

## 4. 毎日の流れ

| 時刻 | 処理 | コマンド |
|---|---|---|
| 18:00 | 株価取得 → スクリーニング → 判定 → 翌朝の注文を確定 → 土曜は再学習 | `python run.py cycle` |
| 08:30 | 安全装置を通した注文を送信（寄付の成行）。paper では何もしない | `python run.py orders` |
| 翌 18:00 | （live）約定結果を口座へ反映してから次の判定 | `cycle` に含まれる |

`python run.py loop` を起動しておけば上記を自動で行います。

---

## 5. 安全装置

| 装置 | 設定 | 動作 |
|---|---|---|
| 緊急停止ファイル | `python run.py stop` / `resume` | `STOP` があれば一切発注しない |
| 資産の下限 | `risk.haltDrawdownPct` | 初期資金比でこの%下落したら新規買いを停止 |
| 連敗停止 | `risk.maxConsecLosses` | 連敗数がこれに達したら新規買いを停止 |
| 1日の買い回数 | `risk.maxDailyTrades` | 超えたら翌日まで買わない |
| 現金の温存 | `risk.reserveCashPct` | 初期資金の一定割合は常に現金で残す |
| 1注文の上限 | `guard.max_order_value_yen` | 超える注文は送らない |
| 発注時刻の範囲 | `guard.send_window` | 範囲外では送らない |
| 監視対象外 | 自動 | スクリーナー候補と保有銘柄以外には買い注文を出さない |
| 借入の禁止 | 常時 | 信用取引は使わず、発注は保有現金の範囲内のみ |

保有中の建玉は、停止中でも損切り・トレンド崩れの条件で通常どおり手仕舞います。

---

## 6. 「学習」について正直に

`learn` は値動きを予測するものではありません。期間を分割し、前半で最適化した設定を**後半の見ていない期間**で運用して成績を測り（ウォークフォワード検証）、現在の設定を上回り、かつプラスのときだけ採用します。それ以外は現状維持です。

擬似データ（ランダムに近い値動き）では、どの設定も上昇相場で買い持ちに勝てませんでした。この戦略の価値は下落相場で損失を避けることにあります。実データでの検証結果を見て判断してください。

---

## 7. ファイル構成

```
run.py                        コマンド入口
config.yaml                   設定（init で生成。認証情報を含むため共有しない）
universe.csv                  監視銘柄
autotrader/
  simulator.py  strategy.py  indicators.py   売買エンジン（ブラウザ版と同一ロジック）
  screener.py                 全銘柄スクリーニング
  learner.py                  ウォークフォワード再学習
  runner.py                   日次サイクル
  guard.py                    安全装置
  broker/paper.py  kabu.py    証券会社接続
  data/stooq.py  jquants.py  csvdir.py   株価取得
  dashboard.py                状況確認画面
state/                        口座状態・ログ（自動生成）
tests/                        自動テスト（ブラウザ版との一致検証を含む）
```

テストの実行: `cd tests && python -m pytest -q`
